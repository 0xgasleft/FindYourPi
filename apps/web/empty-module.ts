// Intentionally empty. Used as a Turbopack browser-side resolveAlias target
// for optional Node-only dependencies that some libraries reference but
// never actually reach in the browser bundle — see next.config.mjs.
export {};
