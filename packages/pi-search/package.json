{
  "name": "@pi-hunter/pi-search",
  "private": true,
  "version": "0.1.0",
  "description": "π digit generation (reproducible, self-computed), chunked Merkle commitment, and substring search index.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "generate": "tsx scripts/generate-dataset.ts",
    "verify": "tsx scripts/independent-verify.ts",
    "benchmark": "tsx scripts/benchmark-search.ts"
  },
  "dependencies": {
    "@pi-hunter/gmp-native": "workspace:*",
    "@pi-hunter/pi-core": "workspace:*",
    "viem": "2.21.19"
  },
  "devDependencies": {
    "@pi-hunter/config": "workspace:*",
    "tsx": "4.19.1",
    "typescript": "5.5.3",
    "vitest": "2.0.5"
  }
}
