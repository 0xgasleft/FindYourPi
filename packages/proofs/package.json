{
  "name": "@pi-hunter/proofs",
  "private": true,
  "version": "0.1.0",
  "description": "Merkle chunk proof construction + a standalone verifier for π occurrences, independent of apps/api  -  the 'don't trust our backend' escape hatch (docs/proof-system.md).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "benchmark": "node benchmark/chunk-encoding.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@pi-hunter/pi-core": "workspace:*",
    "@pi-hunter/pi-search": "workspace:*",
    "viem": "2.21.19"
  },
  "devDependencies": {
    "@pi-hunter/config": "workspace:*",
    "typescript": "5.5.3",
    "vitest": "2.0.5"
  }
}
